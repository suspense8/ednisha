import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Contact & Enrollment",
    description:
        "Get in touch with Ednisha Day Care & Preparatory School. Inquire about enrollment, schedule a visit, or contact us via WhatsApp, phone, or email.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
    return children;
}
